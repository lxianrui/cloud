server:
  port: 9000
spring:
  cloud:
    gateway:
      enabled: true
      routes:
        - id: baidu
          uri: http://baidu.com
          predicates:
            - Path=/baidu/**
          filters:
            - name: AuthInterceptor
            - StripPrefix=1